use wgpu::SamplerDescriptor;

pub fn bloom(surface_texture: wgpu::SurfaceTexture, texture_view_descriptor: wgpu::TextureViewDescriptor threshold: f32, size: f32) -> wgpu::SurfaceTexture {
        let texture = surface_texture.texture;
        let texture_2 = texture.;


}
